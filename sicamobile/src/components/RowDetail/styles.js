import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  detailPanelProperty: {
    fontSize: 14,
    color: '#41414d',
    fontWeight: 'bold',
    marginTop: 24,
  },

  detailPanelValue: {
    marginTop: 8,
    fontSize: 15,
    color: '#737380',
  },
});
