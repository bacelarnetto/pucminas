pipeline {

  agent any

  stages {
        stage('Compose Infra') {
            steps {
              sh 'docker stop $(docker ps -aq)'
              sh 'cd sica-infra && docker-compose up &'
            }
        }
    }
}
