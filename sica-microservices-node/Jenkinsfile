pipeline{
    agent any
    stages{
        stage("Deploy Back-end Node.js"){
            steps{
                sh "cd sica-microservices-node && docker-compose up -d --build"
            }
        }
    }   
}
