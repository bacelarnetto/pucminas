pipeline{
    agent any
    stages{

        stage('Build') {
            environment { 
                    AOEU= sh (returnStdout: true, script: 'echo aoeu').trim()
                }
            steps {
                sh 'cd sica-microservices-node && env'
                sh 'cd sica-microservices-node && echo $AOEU'
            }
        }
    }   
}
