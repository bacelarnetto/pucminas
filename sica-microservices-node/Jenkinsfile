pipeline{
    agent any
    stages{
        stage("Deploy Backend Node.js"){
            steps{
                sh "cd sica-microservices-node && docker-compose build"
                sh "cd sica-microservices-node && docker-compose up -d"
            }
        }
    }   
}
